# Basic

The smallest complete web System. It demonstrates one Application, one Feature, one
Presentation, local deployment, and a focused Feature behavior test.

Run it from the repository root:

```sh
nub run dev
```
