# Presentation

A focused web Presentation example for responsive composition, assets,
interaction state, gesture-driven surfaces, coordinated motion, and
interruption.

In a workspace created from this example, run:

```sh
nub run dev
```

While contributing to Kit itself, run it from the repository root:

```sh
nub src/cli.ts dev --dir examples/presentation
```
