kind: breaking
summary: The framework package and TypeScript import name is now simply `kit`.

# Kit Package Name

Import public APIs from `kit` and use `kit` as the JSX import source. The
obsolete package scopes were repository residue rather than framework concepts.
