{
  "name": "@poggers/site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "poggers dev",
    "bundle": "poggers bundle --outdir .app/build/web",
    "build": "poggers build --outfile dist/site",
    "start": "./dist/site",
    "typegen": "poggers typegen",
    "check": "poggers check",
    "typecheck": "poggers typegen && tsc --noEmit"
  },
  "dependencies": {
    "@poggers/kit": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
