[package]
name = "poggers-native-clock"
version = "0.0.0"
edition = "2024"

[dependencies]
poggers-native-runtime = { path = "../../runtime" }

[dev-dependencies]
tokio = { version = "1.48.0", features = ["macros", "rt"] }
