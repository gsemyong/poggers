[package]
name = "poggers-server-clock"
version = "0.0.0"
edition = "2024"

[dependencies]
poggers-server-runtime = { path = "../../runtime" }

[dev-dependencies]
tokio = { version = "1.48.0", features = ["macros", "rt"] }
