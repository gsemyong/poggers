[package]
name = "kit-server-timer"
version = "0.0.0"
edition = "2024"

[dependencies]
kit-server-runtime = { path = "../../runtime" }
serde_json = "1.0.145"
tokio = { version = "1.48.0", features = ["time"] }

[dev-dependencies]
tokio = { version = "1.48.0", features = ["macros", "rt"] }
