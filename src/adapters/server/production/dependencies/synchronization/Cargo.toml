[package]
name = "kit-server-synchronization"
version = "0.0.0"
edition = "2024"

[dependencies]
kit-server-runtime = { path = "../../runtime" }
tokio = { version = "1.48.0", features = ["sync"] }

[dev-dependencies]
tokio = { version = "1.48.0", features = ["macros", "rt", "sync"] }
