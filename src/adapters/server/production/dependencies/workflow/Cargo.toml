[package]
name = "kit-server-workflow"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
async-stream = "0.3.6"
kit-server-runtime = { path = "../../runtime" }
serde_json = "1.0.151"
tokio = { version = "1.53.1", features = ["macros", "rt", "sync"] }
