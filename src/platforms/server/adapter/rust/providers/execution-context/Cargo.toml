[package]
name = "kit-server-execution-context"
version = "0.0.0"
edition = "2024"

[dependencies]
kit-server-runtime = { path = "../../../../../../compiler/rust/runtime" }
tokio = { version = "1.48.0", features = ["rt"] }

[dev-dependencies]
serde_json = "1"
tokio = { version = "1.48.0", features = ["macros", "rt"] }
